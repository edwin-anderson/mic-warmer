import KeyboardShortcuts

extension KeyboardShortcuts.Name {
    // No default value: the app ships with no hotkey, so "Set Hotkey…" is the first step.
    static let toggleWarm = Self("toggleWarm")
}
